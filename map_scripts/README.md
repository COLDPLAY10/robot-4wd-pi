# 🤖 Автономная навигация с лидаром Yahboom T-MINI Plus

Система автономной навигации для мобильной робототехнической платформы на базе Raspberry Pi 5 с поддержкой SLAM, лидара, камеры и ультразвукового датчика.

## ✨ Основные возможности

- 🗺️ **SLAM** - построение карты в реальном времени
- 📡 **Лидар Yahboom T-MINI Plus** - 360° сканирование окружения
- 📷 **Камера с сегментацией** - обнаружение препятствий нейросетью
- 📏 **Ультразвуковой датчик** - дополнительная безопасность
- 🎯 **Автономная навигация** - движение к цели с объездом препятствий
- 🔄 **Два режима работы**:
  - Режим анализа среды (медленное движение с детализацией)
  - Режим навигации по маршруту (быстрое движение к цели)

## 🚀 Быстрый старт

### 1. Настройка лидара

```bash
# Автоматическая настройка UART
sudo ./setup_lidar.sh

# Перезагрузка
sudo reboot

# Диагностика
python3 diagnose_lidar.py

# Тест лидара
python3 lidar.py
```

### 2. Установка зависимостей

```bash
# Способ 1: Через requirements.txt (рекомендуется)
pip3 install -r requirements.txt

# Способ 2: Вручную
# Основные зависимости (обязательно)
pip3 install numpy pyserial opencv-python

# Для визуализации карты (опционально)
pip3 install matplotlib

# Для Yahboom робота (должно быть установлено отдельно)
# Raspbot_Lib - библиотека управления роботом Yahboom
# Устанавливается согласно инструкции производителя
```

**Примечание:** Библиотека `Raspbot_Lib` устанавливается отдельно по инструкции от Yahboom.

### 3. Запуск

```bash
# Перейдите в папку со скриптами
cd map_scripts

# Исследование окружения с лидаром
python3 demo_with_lidar.py explore

# Движение к точке (2.0, 1.5) метров
python3 demo_with_lidar.py goto 2.0 1.5

# Без лидара (только ультразвук + камера)
python3 demo_no_lidar.py explore

# Автономная навигация (полный функционал)
python3 autonomous_navigation.py --lidar --camera --ultrasonic
```

## 📁 Структура проекта

```
robot-4wd-pi/
│
├── 🔧 Основные компоненты
│   ├── lidar.py                    # Драйвер лидара Yahboom T-MINI Plus
│   ├── car_adapter.py              # Адаптер для управления роботом
│   ├── ultrasonic.py               # Ультразвуковой датчик
│   ├── camera.py                   # Модуль работы с камерой
│   ├── obstacle_detector.py        # Обнаружение препятствий
│   └── RGB.py                      # RGB подсветка
│
├── 🗺️ Модуль навигации и SLAM (map/)
│   ├── __init__.py
│   ├── navigation_controller.py    # Главный контроллер навигации
│   ├── slam_core.py                # Ядро SLAM
│   ├── sensor_fusion.py            # Объединение данных сенсоров
│   └── path_planner.py             # Планирование маршрутов (A*, DWA)
│
├── 📷 Модуль сегментации камеры (camera_segmentation/)
│   ├── __init__.py
│   ├── segmentation.py             # Класс CameraSegmentation
│   ├── simple_segmentation.py      # Простая сегментация по цвету
│   ├── config.py                   # Конфигурация камеры
│   ├── utils.py                    # Вспомогательные функции
│   └── README.md                   # Документация модуля
│
├── 🚀 Скрипты запуска (map_scripts/)
│   ├── autonomous_navigation.py    # Автономная навигация (основной)
│   ├── demo_with_lidar.py          # Демонстрация с лидаром
│   ├── demo_no_lidar.py            # Демонстрация без лидара
│   ├── visualize_map.py            # Визуализация карты SLAM
│   ├── QUICKSTART.md               # Быстрый старт
│   └── README.md                   # Документация (этот файл)
│
├── 🛠️ Утилиты и настройка
│   ├── diagnose_lidar.py           # Диагностика лидара
│   ├── setup_lidar.sh              # Настройка UART для лидара
│   ├── run_robot.sh                # Скрипт запуска робота
│   ├── data_collector.py           # Сбор данных
│   ├── drive_ultrasonic.py         # Движение с ультразвуком
│   └── main.py                     # Основной файл запуска
│
├── 📋 Документация
│   ├── requirements.txt            # Зависимости Python
│   ├── COMMANDS_CHEATSHEET.md      # Шпаргалка команд
│   ├── README_SCRIPTS.md           # Описание скриптов
│   ├── DEPENDENCIES.md             # Детали зависимостей
│   └── check_dependencies.py       # Проверка зависимостей
│
└── 📦 Кэш и настройки
    ├── __pycache__/                # Кэш Python
    ├── .git/                       # Git репозиторий
    ├── .gitignore                  # Исключения Git
    └── .idea/                      # Настройки IDE
```

## 💡 Примеры использования

### Простой тест лидара

```python
from lidar import LidarDriver
import time

with LidarDriver('/dev/ttyAMA0') as lidar:
    lidar.start_scan()
    time.sleep(1)
    
    scan = lidar.get_scan()
    print(f"Получено {len(scan)} точек")
```

### Навигация в коде

```python
from map.navigation_controller import NavigationController
import time

# Создание контроллера с лидаром
controller = NavigationController(
    use_lidar=True,
    use_camera=True,
    use_ultrasonic=True
)

# Установка цели
controller.set_goal(x=2.0, y=1.5)

# Основной цикл
try:
    while True:
        controller.update()
        
        status = controller.get_status()
        if status['mode'] == 'idle':
            print("Цель достигнута!")
            break
            
        time.sleep(0.05)
finally:
    controller.stop()
    controller.save_map("map.png")
```

### Обнаружение препятствий

```python
from lidar import LidarDriver
import numpy as np

lidar = LidarDriver()
lidar.connect()
lidar.start_scan()

# Проверка спереди (±30°)
front = lidar.get_closest_obstacle(-np.pi/6, np.pi/6)
if front and front[1] < 0.5:
    print(f"ВНИМАНИЕ: препятствие на {front[1]:.2f}м!")

lidar.disconnect()
```

## 🎮 Режимы работы

### 1. Режим анализа среды
Медленное движение с максимальной детализацией сенсорных данных.

```bash
cd map_scripts
python3 demo_with_lidar.py explore
```

### 2. Режим навигации по маршруту
Быстрое движение к цели по предварительно рассчитанному маршруту.

```bash
cd map_scripts
python3 demo_with_lidar.py goto X Y
```

### 3. Автономная навигация
Полный функционал с настройкой сенсоров.

```bash
cd map_scripts
python3 autonomous_navigation.py --help
```

## 🔍 Диагностика

### Проверка лидара

```bash
# Комплексная диагностика
python3 diagnose_lidar.py
```

Проверяет:
- ✓ Наличие UART портов
- ✓ Права доступа
- ✓ Установку зависимостей
- ✓ Подключение лидара
- ✓ Получение данных
- ✓ Вывод примеров

### Простой тест

```bash
# Тест драйвера
python3 lidar.py
```

## ⚙️ Настройка UART

Лидар подключается через UART. Для работы требуется настройка:

```bash
# Автоматически
sudo ./setup_lidar.sh
sudo reboot

# Или вручную через raspi-config
sudo raspi-config
# → Interface Options → Serial Port
# → Login shell: NO, Hardware: YES
```


